import Foundation

/// 민감 설정값 — git에서 제외됨 (.gitignore).
/// 이 파일을 Secrets.swift 로 복사한 뒤 실제 값을 입력하세요.
enum Secrets {
    static let supabaseURL = "https://<PROJECT_REF>.supabase.co"
    static let supabaseAnonKey = "<ANON_KEY>"
}
